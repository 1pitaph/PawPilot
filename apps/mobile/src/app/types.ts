export type TabKey = "today" | "map" | "spots" | "pet";

export type HomeNoticeKind =
  | "ask"
  | "assistant"
  | "routeSwapped"
  | "feedbackSmooth"
  | "feedbackRisk"
  | "addMode";

export type FeedbackState = "idle" | "smooth" | "risk";

export type AvatarPhotoSource = "camera" | "library" | "demo";
